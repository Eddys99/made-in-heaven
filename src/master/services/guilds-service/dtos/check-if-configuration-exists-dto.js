class CheckIfConfigurationExistsDTO {
    constructor(discord_user_id, server_id) {
        this.discord_user_id = discord_user_id;
        this.server_id = server_id;
    }
}

module.exports = CheckIfConfigurationExistsDTO;
