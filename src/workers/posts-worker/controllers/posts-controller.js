const $LABEL = 'PostsController';

class PostsController {

    static makePost(request, response, next) {
        const $JOB_LABEL = 'makePost', $LOG_LABEL = `[${$LABEL}][${$JOB_LABEL}]`;

        //make post here ...
    }
}

module.exports = PostsController;
